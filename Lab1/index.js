const Emely = require("./Emely.js");
//inheritance: Person->Dan (is a) person which is the concept of extends
  let p = new Emely("Emely", "cyan");
//sent two arguments to the constructor (name/color) with more specific data

//console.log(p.name);
p.speak();
//console.log(p.speak());
